# Esta carpeta guarda usuarios.json y puntuaciones.json (PIN con hash,
# pero aun así no deben poder descargarse directamente).
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
